#include <stdio.h>

/*int main(int argc, char **argv) {

	printf("Hello Ruth\n");
	printf("How are you?\n");

	//Declarations
	int x;
	char y;
	double z;

	//Assignments
	x = 3;
	y = 'B'; //when specifying a character we use single quotes
	z = 3.14;

	printf("X = %d\n", x); // %d - print out value x as a decimal
	printf("Y = %c\n", y); //%c - print out value y as a character
	printf("Z = %.4f\n", z); //%f - print as float,  %.nf - print with n decimal

	//x = 3
	x++; // postIncrement x = x + 1 or x+= 1; after the line is complete it adds
	printf("X = %d\n", x);
	++x; // PreIncrement x = x + 1; before the line if complete it adds
	printf("X = %d\n", x);

	//if we are doing arithmetic operations on the same line
	//post and pre increment will vary!

	//x = 5
	int a = 4;
	int checkPre = ++x+a; // x = 6, a = 4
	printf("X = %d\n", x);
	int checkPost = a+x++; //  x = 6, a = 4
	printf("checkPre = %d\n", checkPre);
	printf("checkPost = %d\n",checkPost);
	printf("X = %d\n", x);

	//Conditions
	int nimaPresent = 1;
	int nimaFriendPresent = 0;

	if(nimaPresent && nimaFriendPresent) {
		printf("Nima and friend watch movie!!");
	}else {
		printf("Nima goes bowling!");
		return -1;
	}

	return 0;
}*/
