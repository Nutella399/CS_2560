#include<stdio.h>

/*int* reverse_array(int array[], int size) {
	static int copy_array[5];

	for(int i = 0; i < 5; i++) {
		copy_array[size -(i+1)] = array[i];
	}
	return copy_array;
}

void print_array(int* array, int size){
	for(int i = 0; i < size; i++) {
		printf("%d ", *(array+i));
	}
	printf("\n");
}

int main() {
	int size = 5;
	int array[] = {1,2,3,4,5};

	int* pointer = array;
	int* pointer_copy;
	pointer_copy = reverse_array(array, size);
	print_array(pointer, size);
	print_array(pointer_copy, size);
}*/
